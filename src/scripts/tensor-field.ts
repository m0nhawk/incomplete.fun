export {};
const canvas=document.querySelector<HTMLCanvasElement>("#tensor-canvas"),aniso=document.querySelector<HTMLInputElement>("#tensor-aniso"),source=document.querySelector<HTMLInputElement>("#tensor-source"),random=document.querySelector<HTMLButtonElement>("#tensor-random"),readout=document.querySelector<HTMLElement>("#tensor-readout");
let seed=3;if(canvas&&aniso&&source&&random&&readout){const ctx=canvas.getContext("2d");if(ctx)init(ctx)}
function init(ctx:CanvasRenderingContext2D){const resize=()=>{const r=canvas!.getBoundingClientRect(),s=devicePixelRatio||1;canvas!.width=Math.max(320,Math.floor(r.width*s));canvas!.height=Math.max(240,Math.floor(r.height*s));draw(ctx)};random!.onclick=()=>{seed+=17;draw(ctx)};aniso!.oninput=()=>draw(ctx);source!.oninput=()=>draw(ctx);canvas!.onpointerdown=e=>trace(ctx,e);new ResizeObserver(resize).observe(canvas!.parentElement??canvas!);resize()}
function field(x:number,y:number){const a=Number(aniso!.value)/100,b=Number(source!.value)/100;let vx=Math.cos(seed+x*3-y*2)+a*(x-y),vy=Math.sin(seed+y*4+x)+b*(x+y);const angle=.5*Math.atan2(2*vx*vy,vx*vx-vy*vy+.001);return{angle,mag:Math.hypot(vx,vy)}}
function draw(ctx:CanvasRenderingContext2D){const W=canvas!.width,H=canvas!.height;ctx.fillStyle="#03040a";ctx.fillRect(0,0,W,H);for(let y=24;y<H;y+=28)for(let x=24;x<W;x+=28){const f=field(x/W*2-1,y/H*2-1),l=7+Math.min(18,f.mag*4),c=`hsla(${190+f.angle*120},85%,65%,.65)`;ctx.strokeStyle=c;ctx.beginPath();ctx.moveTo(x-Math.cos(f.angle)*l,y-Math.sin(f.angle)*l);ctx.lineTo(x+Math.cos(f.angle)*l,y+Math.sin(f.angle)*l);ctx.stroke();ctx.strokeStyle="rgba(255,255,255,.18)";ctx.beginPath();ctx.moveTo(x-Math.cos(f.angle+Math.PI/2)*l*.55,y-Math.sin(f.angle+Math.PI/2)*l*.55);ctx.lineTo(x+Math.cos(f.angle+Math.PI/2)*l*.55,y+Math.sin(f.angle+Math.PI/2)*l*.55);ctx.stroke()}for(let i=0;i<12;i++)traceFrom(ctx,(i/11)*2-1,-.9+i%2*.18);readout!.textContent="glyphs · separatrices · click to trace"}
function trace(ctx:CanvasRenderingContext2D,e:PointerEvent){const r=canvas!.getBoundingClientRect();traceFrom(ctx,(e.clientX-r.left)/r.width*2-1,(e.clientY-r.top)/r.height*2-1)}
function traceFrom(ctx:CanvasRenderingContext2D,x:number,y:number){const W=canvas!.width,H=canvas!.height;ctx.strokeStyle="rgba(255,238,150,.65)";ctx.beginPath();ctx.moveTo((x*.5+.5)*W,(y*.5+.5)*H);for(let i=0;i<90;i++){const f=field(x,y);x+=Math.cos(f.angle)*.018;y+=Math.sin(f.angle)*.018;ctx.lineTo((x*.5+.5)*W,(y*.5+.5)*H)}ctx.stroke()}
