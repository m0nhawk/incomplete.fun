export {};
const canvas=document.querySelector<HTMLCanvasElement>("#optics-canvas"),slits=document.querySelector<HTMLInputElement>("#optics-slits"),width=document.querySelector<HTMLInputElement>("#optics-width"),button=document.querySelector<HTMLButtonElement>("#optics-draw"),readout=document.querySelector<HTMLElement>("#optics-readout");
if(canvas&&slits&&width&&button&&readout){const ctx=canvas.getContext("2d");if(ctx)init(ctx)}
function init(ctx:CanvasRenderingContext2D){const reset=()=>{slits!.value="2";width!.value="14";draw(ctx)};const randomize=()=>{slits!.value=String(1+Math.random()*7|0);width!.value=String(6+Math.random()*30|0);draw(ctx)};const resize=()=>{const r=canvas!.getBoundingClientRect(),s=devicePixelRatio||1;canvas!.width=Math.max(320,Math.floor(r.width*s));canvas!.height=Math.max(240,Math.floor(r.height*s));draw(ctx)};slits!.oninput=()=>draw(ctx);width!.oninput=()=>draw(ctx);button!.onclick=randomize;window.addEventListener("incomplete:reset",reset);window.addEventListener("incomplete:randomize",randomize);canvas!.onpointerdown=e=>paint(ctx,e);new ResizeObserver(resize).observe(canvas!.parentElement??canvas!);resize()}
function aperture(x:number,n:number,w:number){let a=0;for(let i=0;i<n;i++){const c=(i-(n-1)/2)*.22;if(Math.abs(x-c)<w)a=1}return a}
function intensity(kx:number,ky:number,n:number,w:number){let re=0,im=0;for(let y=-28;y<=28;y++)for(let x=-28;x<=28;x++){const xx=x/56,yy=y/56,a=aperture(xx,n,w)*(Math.abs(yy)<.36?1:0);const ph=kx*xx+ky*yy;re+=a*Math.cos(ph);im+=a*Math.sin(ph)}return Math.log1p((re*re+im*im)/900)}
function draw(ctx:CanvasRenderingContext2D){const W=canvas!.width,H=canvas!.height,n=Number(slits!.value),sw=Number(width!.value)/300;ctx.fillStyle="#03040a";ctx.fillRect(0,0,W,H);const mid=W*.34;ctx.fillStyle="rgba(255,255,255,.08)";ctx.fillRect(mid-2,0,4,H);ctx.fillStyle="#f8f7d2";for(let y=0;y<H;y+=2)for(let x=0;x<mid;x+=2){const xx=(x/mid-.5),yy=(y/H-.5);if(aperture(xx,n,sw)&&Math.abs(yy)<.36)ctx.fillRect(x,y,2,2)}for(let y=0;y<H;y+=3)for(let x=mid+8;x<W;x+=3){const kx=(x-(mid+8))/(W-mid-8)*52-26,ky=(y/H-.5)*52,v=intensity(kx,ky,n,sw);ctx.fillStyle=`hsl(${220-v*80},90%,${Math.min(70,8+v*22)}%)`;ctx.fillRect(x,y,3,3)}readout!.textContent=`${n} slit aperture · Fourier magnitude fringes`}
function paint(ctx:CanvasRenderingContext2D,e:PointerEvent){const r=canvas!.getBoundingClientRect();ctx.strokeStyle="rgba(255,255,255,.75)";ctx.beginPath();ctx.arc((e.clientX-r.left)/r.width*canvas!.width,(e.clientY-r.top)/r.height*canvas!.height,18,0,Math.PI*2);ctx.stroke()}
