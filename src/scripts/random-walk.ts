export {};
const canvas=document.querySelector<HTMLCanvasElement>("#walk-canvas"),count=document.querySelector<HTMLInputElement>("#walk-count"),domain=document.querySelector<HTMLSelectElement>("#walk-domain"),reset=document.querySelector<HTMLButtonElement>("#walk-reset"),readout=document.querySelector<HTMLElement>("#walk-readout");
const W=96,H=96;if(canvas&&count&&domain&&reset&&readout){const ctx=canvas.getContext("2d");if(ctx)init(ctx)}
function init(ctx:CanvasRenderingContext2D){let walkers:{x:number;y:number}[]=[],hits=new Uint16Array(W*H),steps=0;const seed=()=>{walkers=[];hits.fill(0);steps=0;for(let i=0;i<Number(count!.value);i++)walkers.push({x:W/2|0,y:H/2|0})};const resize=()=>{const r=canvas!.getBoundingClientRect(),s=devicePixelRatio||1;canvas!.width=Math.max(320,Math.floor(r.width*s));canvas!.height=Math.max(240,Math.floor(r.height*s));ctx.imageSmoothingEnabled=false};reset!.onclick=seed;count!.oninput=seed;domain!.onchange=seed;new ResizeObserver(resize).observe(canvas!.parentElement??canvas!);resize();seed();const img=ctx.createImageData(W,H);const tick=()=>{for(let k=0;k<6;k++){for(const p of walkers)move(p,hits);steps++}draw(ctx,img,hits,walkers,steps);requestAnimationFrame(tick)};tick()}
function move(p:{x:number;y:number},hits:Uint16Array){const dirs=domain!.value==="hex"?[[1,0],[-1,0],[0,1],[0,-1],[1,-1],[-1,1]]:domain!.value==="triangle"?[[1,0],[-1,0],[0,1],[0,-1],[1,1],[-1,-1]]:[[1,0],[-1,0],[0,1],[0,-1]];const d=dirs[Math.random()*dirs.length|0];p.x=Math.max(0,Math.min(W-1,p.x+d[0]));p.y=Math.max(0,Math.min(H-1,p.y+d[1]));hits[p.y*W+p.x]++}
function draw(ctx:CanvasRenderingContext2D,img:ImageData,hits:Uint16Array,walkers:{x:number;y:number}[],steps:number){let covered=0,max=1;for(const h of hits){if(h)covered++;if(h>max)max=h}for(let i=0;i<hits.length;i++){const v=Math.log1p(hits[i])/Math.log1p(max),p=i*4;img.data[p]=v*80;img.data[p+1]=v*210;img.data[p+2]=80+v*175;img.data[p+3]=255}const off=new OffscreenCanvas(W,H);off.getContext("2d")!.putImageData(img,0,0);ctx.drawImage(off,0,0,canvas!.width,canvas!.height);ctx.fillStyle="rgba(255,255,255,.85)";for(const p of walkers.slice(0,80))ctx.fillRect(p.x/W*canvas!.width,p.y/H*canvas!.height,2,2);readout!.textContent=`${domain!.value} · covered ${(covered/(W*H)*100).toFixed(1)}% · t=${steps}`}
