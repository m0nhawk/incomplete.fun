export {};
const canvas=document.querySelector<HTMLCanvasElement>("#cel-canvas"),ratio=document.querySelector<HTMLInputElement>("#cel-ratio"),ecc=document.querySelector<HTMLInputElement>("#cel-ecc"),clear=document.querySelector<HTMLButtonElement>("#cel-clear"),readout=document.querySelector<HTMLElement>("#cel-readout");
if(canvas&&ratio&&ecc&&clear&&readout){const ctx=canvas.getContext("2d");if(ctx)init(ctx)}
function init(ctx:CanvasRenderingContext2D){let t=0,points:{x:number;y:number}[]=[];const resize=()=>{const r=canvas!.getBoundingClientRect(),s=devicePixelRatio||1;canvas!.width=Math.max(320,Math.floor(r.width*s));canvas!.height=Math.max(240,Math.floor(r.height*s));points=[]};clear!.onclick=()=>points=[];ratio!.oninput=()=>points=[];ecc!.oninput=()=>points=[];new ResizeObserver(resize).observe(canvas!.parentElement??canvas!);resize();const tick=()=>{for(let i=0;i<5;i++){t+=.012;sample(t,points)}draw(ctx,t,points);requestAnimationFrame(tick)};tick()}
function sample(t:number,pts:{x:number;y:number}[]){const r=Number(ratio!.value)/100,e=Number(ecc!.value)/100;if(Math.abs((t%(Math.PI*2))-.01)<.04){pts.push({x:Math.cos(t*r)*(1+e*Math.cos(t)),y:Math.sin(t*r)*(1+e*Math.sin(t*1.7))});if(pts.length>900)pts.shift()}}
function draw(ctx:CanvasRenderingContext2D,t:number,pts:{x:number;y:number}[]){const W=canvas!.width,H=canvas!.height,cx=W*.38,cy=H*.5,scale=Math.min(W,H)*.18,r=Number(ratio!.value)/100,e=Number(ecc!.value)/100;ctx.fillStyle="rgba(3,4,10,.2)";ctx.fillRect(0,0,W,H);ctx.strokeStyle="rgba(255,255,255,.13)";for(const rad of [1,1.6,2.2]){ctx.beginPath();ctx.arc(cx,cy,scale*rad,0,Math.PI*2);ctx.stroke()}ctx.fillStyle="#ffd36b";ctx.beginPath();ctx.arc(cx,cy,8,0,Math.PI*2);ctx.fill();const p1=orbit(t,1,e),p2=orbit(t,r,e*.55);dot(ctx,cx+p1.x*scale,cy+p1.y*scale,"#69d2ff");dot(ctx,cx+p2.x*scale*1.7,cy+p2.y*scale*1.7,"#ff6bb5");const sx=W*.72,sy=H*.5;ctx.strokeStyle="rgba(255,255,255,.18)";ctx.strokeRect(sx-scale,sy-scale,scale*2,scale*2);for(const p of pts){ctx.fillStyle=`hsla(${210+p.x*80},85%,65%,.65)`;ctx.fillRect(sx+p.x*scale*.75,sy+p.y*scale*.75,2,2)}readout!.textContent=`${r.toFixed(2)}:1 · ${near(r)} · Poincaré dots ${pts.length}`}
function orbit(t:number,w:number,e:number){const a=t*w;return{x:Math.cos(a)*(1+e*Math.cos(a)),y:Math.sin(a)*(1-e*.4)}}function dot(ctx:CanvasRenderingContext2D,x:number,y:number,c:string){ctx.fillStyle=c;ctx.beginPath();ctx.arc(x,y,5,0,Math.PI*2);ctx.fill()}function near(r:number){const vals=[[2,"2:1"],[2.5,"5:2"],[3,"3:1"],[1.5,"3:2"],[4,"4:1"]];let best=vals[0];for(const v of vals)if(Math.abs(r-v[0])<Math.abs(r-best[0]))best=v;return best[1]+" resonance"}
